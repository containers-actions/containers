# Subversion Container Image

Subversion (SVN) client in a container.

## Tags

- `latest`
- Version-specific tags

## Usage

```bash
docker run --rm -it ghcr.io/containers-actions/subversion:latest svn --version
```

## Build Information

- Base Image: bitnami/os-shell
- Install Path: /usr/local/bin